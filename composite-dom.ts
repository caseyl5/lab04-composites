interface IDomElement {
  print();
}

// only prints tag 
// calls dom elemnt if tag is not present 
class DomElement implements IDomElement {
   
   // print + DomElement + inner component of list 
}

// prints HI, I am a textnode being printed 
class TextNode extends DomElement {

}
